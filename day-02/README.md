# 📅 Day 2 – Add Two Numbers.

Hello! 👋, Welcome to Day 2 of my 50 Days **JavaScript Problem-Solving Challenge 🚀.**

I'm Rakibul Rahman from RR-Dev.

## 🎯 Today Task:

Today's task is very simple, write a function that takes two input (parameters) and returns their sum.

We following some step's and solve this problem.

Following step's is below:

1. Create a function that name is `sum()`.
2. Inside the function, create variable that name is sum and add two number.
3. Return the sum.
4. Call the function inside the `console.log()` and see output.

**Example:**

```javascript
function sum(a, b) {
	// your code
};

console.log(sum(10, 20));
```

📝 If you don't call the function inside the `console.log()`, you can't see the output.

Like, Share & Follow for more problem-solving post or reels.

**Facebook:** www.facebook.com/rakibulrahman.rrdev

Thank you from **RR-Dev**, Learn. Build. Improve.

See you in Day 3 🔥.
